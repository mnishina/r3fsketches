---
import Layout from "~/layouts/Layout.astro";
import Scene from "~/components/firstScene/Scene";

import { pagesData, getPageTitle } from "~/data/pages";
const title = getPageTitle(pagesData, Astro.url.pathname);
---

<Layout title={title}>
  <Scene client:load />
</Layout>
